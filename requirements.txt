# 发票OCR识别器依赖包列表 - 纯离线版本
# 适用于 PaddleOCR 3.1+ 版本

# 核心OCR依赖（PaddleOCR）
paddleocr>=3.1.0
paddlepaddle

# 图像处理
pillow>=8.0.0
opencv-python-headless>=4.10.0  # 更小的无界面版本，适合服务器/容器

# 数据处理
pandas>=1.3.0
numpy>=1.24.0
openpyxl>=3.1.0  # Excel 导出引擎

# PDF处理
pymupdf>=1.20.0

# GUI界面
pyqt5>=5.15.0

# Web 服务（仅 web 分支需要，GUI 主分支不包含）

# 其他工具
pyyaml>=6.0
typing-extensions>=4.12.0
shapely>=2.0.0
pyclipper>=1.3.0.post5
